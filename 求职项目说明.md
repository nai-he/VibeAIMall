# Vibe智购AI - Android 求职项目说明

## 项目定位

这是一个面向 AI 工程师（Vibe Coding）岗位定制的 Android 购物助手项目。项目以商城业务为底座，接入真实大模型 API，并保留端侧 AI 推荐引擎作为稳定兜底，把用户自然语言需求转成商品推荐、推荐理由、备选方案和购物车动作，最终形成注册登录、推荐、加购、下单、订单状态流转、评价/售后的完整闭环。

## 岗位贴合点

- **Android 客户端开发**：使用 Java 完成首页、分类、购物车、订单、个人中心等核心页面。
- **AI 原生交互**：AI 助手支持“预算3000学习平板”“出差需要续航”“送女生生日礼物”等自然语言输入，配置 Key 后会调用真实大模型生成导购回复。
- **Vibe Coding 思路**：围绕岗位需求快速拆解功能、定位减分项、重构模块并补齐演示文档。
- **产品闭环意识**：推荐不是停留在文本回复，而是能一键加入购物车，并进入下单和订单状态流转。
- **工程化拆分**：统一商品仓库、购物车服务、订单服务，降低重复代码和状态不一致风险。
- **独立交付能力**：项目可本地构建 APK，不依赖后端服务即可完成主要演示。

## 核心功能

1. 本地账号系统
   - `UserDBOpenHelper` 创建 `users.db`
   - 注册写入 SQLite
   - 登录读取 SQLite

2. AI 智购助手
   - `AiIntentParser` 识别预算、场景、品类
   - `RecommendationEngine` 按商品标签、预算、销量和场景打分
   - `AiRemoteAdvisor` 调用兼容 Chat Completions 的真实大模型 API
   - `AiShoppingChatEngine` 识别“就买这个”“买备选1”“去购物车”等对话购买指令
   - 输出主推商品、备选方案、推荐理由、购买建议
   - 支持按钮加购，也支持对话直接加购

3. 推荐日志闭环
   - `recommend_logs` 记录用户输入、识别意图、推荐商品、动作和时间
   - `RecommendationLogActivity` 可在 App 内查看推荐记录和加购动作
   - 可用于后续扩展推荐转化分析

4. 商城业务闭环
   - 商品列表
   - 购物车
   - 下单/订单
   - 订单状态筛选：待付款、待收货、待评价、退款/售后
   - 订单动作：去付款、确认收货、去评价、查看售后进度
   - 待评价订单进入评价页，提交后写入商品评论并流转为已完成
   - 评论与评价

5. 产品化页面
   - 发现页改造成 AI 产品实验室
   - 个人中心订单状态入口全部可点击
   - 小游戏入口修复运行和退出问题

## 面试讲法

可以这样介绍：

“这个项目原本是一个 Android 商城，我按 AI 工程师岗位做了产品化改造。核心改造是把传统搜索升级成 AI 购物助手：用户用自然语言描述预算、品类和场景，客户端先完成稳定的商品匹配，再调用真实大模型 API 生成更自然的导购回复，并直接打通加入购物车、下单和订单状态流转。为了让项目更像真实产品，我还补了推荐日志、订单状态筛选、发现页场景入口和演示文档。当前版本即使没有网络或 Key，也会自动降级到端侧推荐，保证面试现场可稳定演示。”

## 终极优化补充

- `ProductRepository` 统一首页、分类、详情页和 AI 推荐商品数据。
- `CartService` 统一普通商品和 AI 推荐商品的加购逻辑。
- `AiRemoteAdvisor` 恢复真实大模型 API 能力，并通过本机配置注入 Key，避免源码泄露。
- `AiShoppingChatEngine` 让 AI 推荐页从按钮式交互升级为对话式购物。
- `OrderService` 统一订单查询、演示订单初始化和状态更新。
- `OrderReviewActivity` 打通待评价到评价完成。
- `RecommendationLogActivity` 支持查看 AI 推荐闭环记录。
